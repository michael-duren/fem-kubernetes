apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-app
  namespace: app
spec:
  project: default
  source:
    repoURL: https://github.com/michael-duren/fem-kubernetes
    targetRevision: main
    path: manifests/day-one/k8s/base
  destination:
    server: https://kubernetes.default.svc
    namespace: app
  syncPolicy:
    automated: { prune: true, selfHeal: true }
